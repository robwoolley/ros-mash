# Packaging metadata lives in pyproject.toml. This file only holds config for
# tools colcon/ci's shared workflow reads from a hardcoded setup.cfg path.
[flake8]
import-order-style = google

[coverage:run]
source = mash
